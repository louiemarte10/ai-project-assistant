<?php
/**
 * Template for the Gemini API key. Copy this to `secret-gemini.php` (which is
 * gitignored) and put the real, ROTATED key below. `secret-gemini.php` lives in
 * public/api/ so Vite copies it into dist/ and the deploy uploads it to the
 * server — it is never committed to git.
 */
return 'your-rotated-gemini-key-here';
?>
